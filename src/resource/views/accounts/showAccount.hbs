<link rel="stylesheet" href="/css/showcategories.css">
<div class="background-body">
    <h3 class="title-muc">Show staffs</h3>
</div>
<div class="body-ct">
    <div class="contain-ct">
        <table id="example" class="table table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Password</th>
                    <th>Role</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                {{#if result}}
                {{#each result as |value| }}
                <tr>
                    <td>{{value._id}}</td>
                    <td>{{value.name}}</td>
                    <td>{{value.email}}</td>
                    <td>{{value.password}}</td>
                    <td>{{value.role}}</td>
                    <td class="func-ct">
                        <a href="/managerAccounts/update/{{value._id}}">
                            <i class="fas fa-pen update-ct"></i>
                        </a>
                    </td>
                    <td class="func-ct">
                        <i data-id="{{value._id}}" data-name="{{value.name}}"
                            class="fas fa-trash-alt delete-ct d-staff"></i>
                    </td>
                    {{/each}}
                    {{/if}}
            </tbody>
        </table>
    </div>
</div>